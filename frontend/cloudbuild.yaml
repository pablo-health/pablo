steps:
  - name: 'gcr.io/kaniko-project/executor:latest'
    args:
      - '--dockerfile=Dockerfile.production'
      - '--context=.'
      - '--destination=${_REGION}-docker.pkg.dev/$PROJECT_ID/pablo/frontend:latest'
      - '--cache=true'
      - '--cache-ttl=720h'

substitutions:
  _REGION: 'us-central1'

options:
  machineType: 'E2_HIGHCPU_8'
  logging: CLOUD_LOGGING_ONLY
