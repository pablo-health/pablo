diff --git a/node_modules/next-firebase-auth-edge/browser/auth/credential.js b/node_modules/next-firebase-auth-edge/browser/auth/credential.js
index da75457..a8e3060 100644
--- a/node_modules/next-firebase-auth-edge/browser/auth/credential.js
+++ b/node_modules/next-firebase-auth-edge/browser/auth/credential.js
@@ -124,6 +124,11 @@ export class ComputeEngineCredential {
         if (this.projectId) {
             return Promise.resolve(this.projectId);
         }
+        const explicit = getExplicitProjectId() || process.env.FIREBASE_PROJECT_ID;
+        if (explicit) {
+            this.projectId = explicit;
+            return this.projectId;
+        }
         const url = `http://${GOOGLE_METADATA_SERVICE_HOST}${GOOGLE_METADATA_SERVICE_PROJECT_ID_PATH}`;
         const request = this.buildRequest();
         try {
diff --git a/node_modules/next-firebase-auth-edge/browser/auth/index.js b/node_modules/next-firebase-auth-edge/browser/auth/index.js
index 9b5560e..7335049 100644
--- a/node_modules/next-firebase-auth-edge/browser/auth/index.js
+++ b/node_modules/next-firebase-auth-edge/browser/auth/index.js
@@ -96,7 +96,7 @@ const refreshExpiredIdToken = async (refreshToken, options) => {
             'Content-Type': 'application/x-www-form-urlencoded',
             ...(options.referer ? { Referer: options.referer } : {})
         },
-        body: `grant_type=refresh_token&refresh_token=${refreshToken}`
+        body: `grant_type=refresh_token&refresh_token=${encodeURIComponent(refreshToken)}`
     });
     if (!response.ok) {
         const data = await response.json();
diff --git a/node_modules/next-firebase-auth-edge/browser/next/cookies/index.js b/node_modules/next-firebase-auth-edge/browser/next/cookies/index.js
index be336f5..d9af48e 100644
--- a/node_modules/next-firebase-auth-edge/browser/next/cookies/index.js
+++ b/node_modules/next-firebase-auth-edge/browser/next/cookies/index.js
@@ -16,7 +16,7 @@ export async function appendAuthCookies(headers, response, value, options) {
     await authCookies.setAuthHeaders(value, response.headers);
 }
 export async function setAuthCookies(headers, options) {
-    const { getCustomIdAndRefreshTokens } = getFirebaseAuth({
+    const { getCustomIdAndRefreshTokens, handleTokenRefresh, verifyIdToken } = getFirebaseAuth({
         serviceAccount: options.serviceAccount,
         apiKey: options.apiKey,
         tenantId: options.tenantId
@@ -32,11 +32,46 @@ export async function setAuthCookies(headers, options) {
     }
     const appCheckToken = headers.get('X-Firebase-AppCheck') ?? undefined;
     const referer = getReferer(headers) ?? '';
-    const customTokens = await getCustomIdAndRefreshTokens(token, {
-        appCheckToken,
-        referer,
-        dynamicCustomClaimsKeys: options.dynamicCustomClaimsKeys
-    });
+    // Keyless login for hosts without the GCP metadata server (e.g. AWS): when
+    // the client supplies its refresh token via X-Refresh-Token, store it
+    // directly instead of minting an SA-signed custom token to obtain one. We
+    // still verify the idToken, and confirm the refresh token resolves to the
+    // same uid before trusting it. Uses only the securetoken endpoint (apiKey)
+    // + public-cert verification — no service-account signing.
+    const clientRefreshToken = headers.get('X-Refresh-Token') ?? undefined;
+    let customTokens;
+    if (clientRefreshToken && process.env.NFAE_ALLOW_CLIENT_REFRESH_TOKEN === 'true') {
+        try {
+            const providedDecoded = await verifyIdToken(token, { referer });
+            const refreshed = await handleTokenRefresh(clientRefreshToken, { referer });
+            if (refreshed.decodedIdToken.uid !== providedDecoded.uid) {
+                return new NextResponse(JSON.stringify({ success: false, message: 'Unauthorized' }), {
+                    status: 401,
+                    headers: { 'content-type': 'application/json' }
+                });
+            }
+            customTokens = {
+                idToken: refreshed.idToken,
+                refreshToken: refreshed.refreshToken,
+                customToken: ''
+            };
+        }
+        catch {
+            // Don't leak the underlying Firebase/verify error (or a 500 stack)
+            // as an oracle — any failure here is an auth failure.
+            return new NextResponse(JSON.stringify({ success: false, message: 'Unauthorized' }), {
+                status: 401,
+                headers: { 'content-type': 'application/json' }
+            });
+        }
+    }
+    else {
+        customTokens = await getCustomIdAndRefreshTokens(token, {
+            appCheckToken,
+            referer,
+            dynamicCustomClaimsKeys: options.dynamicCustomClaimsKeys
+        });
+    }
     debug('Successfully generated custom tokens');
     const decodedIdToken = mapJwtPayloadToDecodedIdToken(decodeJwt(customTokens.idToken));
     const metadata = await getMetadataInternal({ ...customTokens, decodedIdToken }, options);
diff --git a/node_modules/next-firebase-auth-edge/esm/auth/credential.js b/node_modules/next-firebase-auth-edge/esm/auth/credential.js
index f16e7d0..6272ef6 100644
--- a/node_modules/next-firebase-auth-edge/esm/auth/credential.js
+++ b/node_modules/next-firebase-auth-edge/esm/auth/credential.js
@@ -130,6 +130,11 @@ export class ComputeEngineCredential {
         if (this.projectId) {
             return Promise.resolve(this.projectId);
         }
+        const explicit = getExplicitProjectId() || process.env.FIREBASE_PROJECT_ID;
+        if (explicit) {
+            this.projectId = explicit;
+            return this.projectId;
+        }
         const url = `http://${GOOGLE_METADATA_SERVICE_HOST}${GOOGLE_METADATA_SERVICE_PROJECT_ID_PATH}`;
         const request = this.buildRequest();
         try {
diff --git a/node_modules/next-firebase-auth-edge/esm/auth/index.js b/node_modules/next-firebase-auth-edge/esm/auth/index.js
index 9b5560e..7335049 100644
--- a/node_modules/next-firebase-auth-edge/esm/auth/index.js
+++ b/node_modules/next-firebase-auth-edge/esm/auth/index.js
@@ -96,7 +96,7 @@ const refreshExpiredIdToken = async (refreshToken, options) => {
             'Content-Type': 'application/x-www-form-urlencoded',
             ...(options.referer ? { Referer: options.referer } : {})
         },
-        body: `grant_type=refresh_token&refresh_token=${refreshToken}`
+        body: `grant_type=refresh_token&refresh_token=${encodeURIComponent(refreshToken)}`
     });
     if (!response.ok) {
         const data = await response.json();
diff --git a/node_modules/next-firebase-auth-edge/esm/next/cookies/index.js b/node_modules/next-firebase-auth-edge/esm/next/cookies/index.js
index be336f5..d9af48e 100644
--- a/node_modules/next-firebase-auth-edge/esm/next/cookies/index.js
+++ b/node_modules/next-firebase-auth-edge/esm/next/cookies/index.js
@@ -16,7 +16,7 @@ export async function appendAuthCookies(headers, response, value, options) {
     await authCookies.setAuthHeaders(value, response.headers);
 }
 export async function setAuthCookies(headers, options) {
-    const { getCustomIdAndRefreshTokens } = getFirebaseAuth({
+    const { getCustomIdAndRefreshTokens, handleTokenRefresh, verifyIdToken } = getFirebaseAuth({
         serviceAccount: options.serviceAccount,
         apiKey: options.apiKey,
         tenantId: options.tenantId
@@ -32,11 +32,46 @@ export async function setAuthCookies(headers, options) {
     }
     const appCheckToken = headers.get('X-Firebase-AppCheck') ?? undefined;
     const referer = getReferer(headers) ?? '';
-    const customTokens = await getCustomIdAndRefreshTokens(token, {
-        appCheckToken,
-        referer,
-        dynamicCustomClaimsKeys: options.dynamicCustomClaimsKeys
-    });
+    // Keyless login for hosts without the GCP metadata server (e.g. AWS): when
+    // the client supplies its refresh token via X-Refresh-Token, store it
+    // directly instead of minting an SA-signed custom token to obtain one. We
+    // still verify the idToken, and confirm the refresh token resolves to the
+    // same uid before trusting it. Uses only the securetoken endpoint (apiKey)
+    // + public-cert verification — no service-account signing.
+    const clientRefreshToken = headers.get('X-Refresh-Token') ?? undefined;
+    let customTokens;
+    if (clientRefreshToken && process.env.NFAE_ALLOW_CLIENT_REFRESH_TOKEN === 'true') {
+        try {
+            const providedDecoded = await verifyIdToken(token, { referer });
+            const refreshed = await handleTokenRefresh(clientRefreshToken, { referer });
+            if (refreshed.decodedIdToken.uid !== providedDecoded.uid) {
+                return new NextResponse(JSON.stringify({ success: false, message: 'Unauthorized' }), {
+                    status: 401,
+                    headers: { 'content-type': 'application/json' }
+                });
+            }
+            customTokens = {
+                idToken: refreshed.idToken,
+                refreshToken: refreshed.refreshToken,
+                customToken: ''
+            };
+        }
+        catch {
+            // Don't leak the underlying Firebase/verify error (or a 500 stack)
+            // as an oracle — any failure here is an auth failure.
+            return new NextResponse(JSON.stringify({ success: false, message: 'Unauthorized' }), {
+                status: 401,
+                headers: { 'content-type': 'application/json' }
+            });
+        }
+    }
+    else {
+        customTokens = await getCustomIdAndRefreshTokens(token, {
+            appCheckToken,
+            referer,
+            dynamicCustomClaimsKeys: options.dynamicCustomClaimsKeys
+        });
+    }
     debug('Successfully generated custom tokens');
     const decodedIdToken = mapJwtPayloadToDecodedIdToken(decodeJwt(customTokens.idToken));
     const metadata = await getMetadataInternal({ ...customTokens, decodedIdToken }, options);
diff --git a/node_modules/next-firebase-auth-edge/lib/auth/credential.js b/node_modules/next-firebase-auth-edge/lib/auth/credential.js
index 50a83f7..7dfbd84 100644
--- a/node_modules/next-firebase-auth-edge/lib/auth/credential.js
+++ b/node_modules/next-firebase-auth-edge/lib/auth/credential.js
@@ -135,6 +135,11 @@ class ComputeEngineCredential {
         if (this.projectId) {
             return Promise.resolve(this.projectId);
         }
+        const explicit = getExplicitProjectId() || process.env.FIREBASE_PROJECT_ID;
+        if (explicit) {
+            this.projectId = explicit;
+            return this.projectId;
+        }
         const url = `http://${GOOGLE_METADATA_SERVICE_HOST}${GOOGLE_METADATA_SERVICE_PROJECT_ID_PATH}`;
         const request = this.buildRequest();
         try {
diff --git a/node_modules/next-firebase-auth-edge/lib/auth/index.js b/node_modules/next-firebase-auth-edge/lib/auth/index.js
index 5e0e9d0..0e99376 100644
--- a/node_modules/next-firebase-auth-edge/lib/auth/index.js
+++ b/node_modules/next-firebase-auth-edge/lib/auth/index.js
@@ -118,7 +118,7 @@ const refreshExpiredIdToken = async (refreshToken, options) => {
             'Content-Type': 'application/x-www-form-urlencoded',
             ...(options.referer ? { Referer: options.referer } : {})
         },
-        body: `grant_type=refresh_token&refresh_token=${refreshToken}`
+        body: `grant_type=refresh_token&refresh_token=${encodeURIComponent(refreshToken)}`
     });
     if (!response.ok) {
         const data = await response.json();
diff --git a/node_modules/next-firebase-auth-edge/lib/next/cookies/index.js b/node_modules/next-firebase-auth-edge/lib/next/cookies/index.js
index 4547893..0c00614 100644
--- a/node_modules/next-firebase-auth-edge/lib/next/cookies/index.js
+++ b/node_modules/next-firebase-auth-edge/lib/next/cookies/index.js
@@ -45,7 +45,7 @@ async function appendAuthCookies(headers, response, value, options) {
     await authCookies.setAuthHeaders(value, response.headers);
 }
 async function setAuthCookies(headers, options) {
-    const { getCustomIdAndRefreshTokens } = (0, index_js_1.getFirebaseAuth)({
+    const { getCustomIdAndRefreshTokens, handleTokenRefresh, verifyIdToken } = (0, index_js_1.getFirebaseAuth)({
         serviceAccount: options.serviceAccount,
         apiKey: options.apiKey,
         tenantId: options.tenantId
@@ -61,11 +61,46 @@ async function setAuthCookies(headers, options) {
     }
     const appCheckToken = headers.get('X-Firebase-AppCheck') ?? undefined;
     const referer = (0, utils_js_1.getReferer)(headers) ?? '';
-    const customTokens = await getCustomIdAndRefreshTokens(token, {
-        appCheckToken,
-        referer,
-        dynamicCustomClaimsKeys: options.dynamicCustomClaimsKeys
-    });
+    // Keyless login for hosts without the GCP metadata server (e.g. AWS): when
+    // the client supplies its refresh token via X-Refresh-Token, store it
+    // directly instead of minting an SA-signed custom token to obtain one. We
+    // still verify the idToken, and confirm the refresh token resolves to the
+    // same uid before trusting it. Uses only the securetoken endpoint (apiKey)
+    // + public-cert verification — no service-account signing.
+    const clientRefreshToken = headers.get('X-Refresh-Token') ?? undefined;
+    let customTokens;
+    if (clientRefreshToken && process.env.NFAE_ALLOW_CLIENT_REFRESH_TOKEN === 'true') {
+        try {
+            const providedDecoded = await verifyIdToken(token, { referer });
+            const refreshed = await handleTokenRefresh(clientRefreshToken, { referer });
+            if (refreshed.decodedIdToken.uid !== providedDecoded.uid) {
+                return new server_1.NextResponse(JSON.stringify({ success: false, message: 'Unauthorized' }), {
+                    status: 401,
+                    headers: { 'content-type': 'application/json' }
+                });
+            }
+            customTokens = {
+                idToken: refreshed.idToken,
+                refreshToken: refreshed.refreshToken,
+                customToken: ''
+            };
+        }
+        catch {
+            // Don't leak the underlying Firebase/verify error (or a 500 stack)
+            // as an oracle — any failure here is an auth failure.
+            return new server_1.NextResponse(JSON.stringify({ success: false, message: 'Unauthorized' }), {
+                status: 401,
+                headers: { 'content-type': 'application/json' }
+            });
+        }
+    }
+    else {
+        customTokens = await getCustomIdAndRefreshTokens(token, {
+            appCheckToken,
+            referer,
+            dynamicCustomClaimsKeys: options.dynamicCustomClaimsKeys
+        });
+    }
     (0, index_js_2.debug)('Successfully generated custom tokens');
     const decodedIdToken = (0, utils_js_2.mapJwtPayloadToDecodedIdToken)((0, jose_1.decodeJwt)(customTokens.idToken));
     const metadata = await (0, metadata_js_1.getMetadataInternal)({ ...customTokens, decodedIdToken }, options);
