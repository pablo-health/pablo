// Copyright (c) 2026 Pablo Health, LLC. Licensed under AGPL-3.0.

/**
 * The forms module's public surface: one component, plus its props.
 *
 * A host mounts `PortalForms` and hands it a live portal session token.
 * Everything else in this directory is the module's own business.
 */

export { PortalForms } from "./PortalForms"
export type { PortalFormsProps } from "./PortalForms"
